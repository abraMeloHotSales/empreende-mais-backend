<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * TODO (quando banco disponível): descomentar os seeders abaixo
     *
     * public function run(): void
     * {
     *     $this->call([
     *         UserSeeder::class,
     *         ModuleSeeder::class,
     *     ]);
     * }
     */
    public function run(): void
    {
        // Banco ainda não configurado — nenhuma seed necessária por enquanto.
    }
}
